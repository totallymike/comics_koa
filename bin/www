#!/usr/bin/env node --harmony
var debug = require('debug')('comics');
var app = require('../app');

var server = app.listen(3000, function() {
  debug('Express server listening on port ' + server.address().port);
});